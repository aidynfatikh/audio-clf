# Copy to .env and fill in. Loaded by loaders/load_data.py via python-dotenv
# at module import.

# HuggingFace Hub — required for datasets/models behind auth (loaders/load_data.py).
HF_TOKEN=

# Google Gemini API — required for comparison/evaluate.py --gemini.
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
